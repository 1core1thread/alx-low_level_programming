#include<stdio.h>
/**
 * main: is always the first function in C
 * puts: allows me to print a long chain of characters
 * return 0: it means that in the end I will get 0 in output
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
